apiVersion: v1
kind: Service
metadata:
  name: kql-simulator
  namespace: kql-dev
spec:
  type: NodePort
  selector:
    app: kql-simulator
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
